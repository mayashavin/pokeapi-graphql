export default `
  type Form {
    id: Int,
    name: String,
    order: Int,
    form_order: Int,
    is_default: Boolean,
    is_battle_only: Boolean,
    is_mega: Boolean,
    form_name: String
  }
`