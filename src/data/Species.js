export default `
  type Species {
    id: Int
    name: String
    order: Int
    gender_rate: Int
    capture_rate: Int
    base_happiness: Int
    is_baby: Boolean
    hatch_counter: Int
    has_gender_differences: Boolean
    forms_switchable: Boolean
  }
`